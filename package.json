{
  "name": "repfl",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "homepage": "https://repfl.ch",
  "dependencies": {
    "concrete.css": "^1.1.1",
    "ky": "^0.11.0",
    "normalize.css": "^8.0.1",
    "preact": "^10.0.0-beta.2"
  },
  "scripts": {
    "start": "parcel src/index.html --port 3000 --hmr-port 1234",
    "lint": "standard --fix",
    "predeploy": "rimraf dist && parcel build --public-url ./ src/index.html",
    "deploy": "echo 'repfl.ch' > ./dist/CNAME && gh-pages -d dist"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.12.3",
    "rimraf": "^2.6.3",
    "standard": "^12.0.1"
  }
}
